{
  "name": "doctor-suplementos-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "mocha firestore.rules.test.js --timeout 20000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "mocha": "^11.0.0"
  }
}
